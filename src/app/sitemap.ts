import type { MetadataRoute } from "next";import { projects } from "@/data/projects";import { getAllPosts } from "@/lib/blog";export default function sitemap():MetadataRoute.Sitemap{const base="https://cemyildiz.net";return [{url:base,lastModified:new Date(),priority:1},{url:`${base}/blog`,lastModified:new Date(),priority:.8},...projects.map(p=>({url:`${base}/work/${p.slug}`,lastModified:new Date(),priority:.8})),...getAllPosts().map(p=>({url:`${base}/blog/${p.slug}`,lastModified:new Date(p.date),priority:.7}))]}
