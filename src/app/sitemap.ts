import type { MetadataRoute } from "next";import { projects } from "@/data/projects";export default function sitemap():MetadataRoute.Sitemap{const base="https://cemyildiz.net";return [{url:base,lastModified:new Date(),priority:1},...projects.map(p=>({url:`${base}/work/${p.slug}`,lastModified:new Date(),priority:.8}))]}
