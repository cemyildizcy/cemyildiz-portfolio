"use client";
import Link from "next/link";
import { useState } from "react";
import { projects } from "@/data/projects";
export function EvidenceDesk(){const [active,setActive]=useState(0);const p=projects[active];return <section id="work" className="desk-section" aria-labelledby="work-title"><div className="section-head"><h2 id="work-title">Three working files.</h2><p>Choose a tab. The evidence sheet changes; the standard does not.</p></div><div className="evidence-desk"><div className="file-tabs" role="tablist" aria-label="Project files">{projects.map((x,i)=><button key={x.slug} role="tab" aria-selected={active===i} aria-controls="evidence-panel" id={`tab-${x.slug}`} onClick={()=>setActive(i)}>{x.title}<span>{x.status}</span></button>)}</div><article id="evidence-panel" role="tabpanel" aria-labelledby={`tab-${p.slug}`} className="proof-sheet"><p className="pencil-note">{p.note}</p><h3>{p.title}</h3><p className="premise">{p.premise}</p><dl><div><dt>What exists</dt><dd>{p.evidence[0]}</dd></div><div><dt>Cem&apos;s part</dt><dd>{p.contribution[0]}</dd></div><div><dt>AI&apos;s part</dt><dd>{p.aiRole}</dd></div></dl><Link className="text-link" href={`/work/${p.slug}`}>Read the evidence note</Link></article></div></section>}
