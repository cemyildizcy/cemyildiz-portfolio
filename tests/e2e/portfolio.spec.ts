import { test,expect } from "@playwright/test";import AxeBuilder from "@axe-core/playwright";test("home exposes evidence and updates the active sheet",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:"I keep the evidence next to the claim."})).toBeVisible();await page.getByRole("tab",{name:/SleepInfo/}).click();await expect(page.getByRole("tabpanel")).toContainText("SleepInfo");});test("approved work routes render",async({page})=>{await page.goto("/work/wc2026-ai-simulator");await expect(page.getByRole("heading",{level:1})).toHaveText("WC2026 AI Simulator");});test("home has no serious accessibility violations or horizontal overflow",async({page})=>{await page.goto("/");const results=await new AxeBuilder({page}).analyze();expect(results.violations.filter(v=>["serious","critical"].includes(v.impact??""))).toEqual([]);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true);});
